[pytest]
asyncio_mode = auto
addopts = -m "not postgres"
testpaths = tests
markers =
    postgres: integration test that requires a real PostgreSQL 16 (Docker/testcontainers); skipped when unavailable